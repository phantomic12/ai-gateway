# Build artifacts
bin/
web/dist/
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# Local data & secrets
data/
.env
.env.local
.env.*.local

# Database backups (live snapshots; root backups/ and deploy/backups/)
backups/

# Compiled gateway binary at repo root (anchored: never ignore cmd/gateway/)
/gateway

# Test scratch output
models_test_output.json

# Dependencies
node_modules/

# Python scratch
__pycache__/

# Editor / AI tool state
.opencode/
.pi/
.catalyst-code/
.swarm/

# Misc scratch
pi-harness-test/hello-pi.txt
.DS_Store

# Per-host deploy overlay + backups (host-specific ports/paths; generated by deploy.sh)
docker-compose.rainbowone.yml.bak
